import { Router } from 'express';
// import { getOrderProducts } from '../controllers/orderProduct.controller';


const router = Router();

// router.get('/', getOrderProducts);

export default router;